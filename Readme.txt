Astro static site for https://www.duitfirst.my

Update `src/data/all-card-data.json`, then run:
1. npm install
2. npm run build

To Preview Built Version:
npmm run preview

Deploy the generated `dist/` output to GitHub Pages.
