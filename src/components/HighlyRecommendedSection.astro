---
import type { Card } from '../types';
import { formatAnnualIncomeRequirement } from '../utils/formatAnnualIncomeRequirement';

interface Props {
  cards: Card[];
  sectionId?: string;
  title?: string;
}

const { cards, sectionId = 'highly-recommended', title = 'Highly Recommended' } = Astro.props;

const recommendedCards = cards.filter((card) => card.recommended);
---

{recommendedCards.length > 0 && (
  <section class="blog-posts section pt-0">
    <style>
      .hide-scrollbar::-webkit-scrollbar {
        display: none;
      }

      .hide-scrollbar {
        -ms-overflow-style: none;
        scrollbar-width: none;
      }

      .scroll-indicator {
        animation: pulse 1.6s ease-in-out infinite;
        opacity: 0.7;
        color: #198754;
      }

      .section-spacer {
        margin-top: 3rem;
      }

      .recommendation-badge {
        display: inline-flex;
        align-items: center;
        align-self: flex-start;
        gap: 0.5rem;
        background: rgba(25, 135, 84, 0.1);
        color: #198754;
        border: 1px solid rgba(25, 135, 84, 0.2);
        border-radius: 999px;
        padding: 0.4rem 0.75rem;
        font-size: 0.82rem;
        font-weight: 700;
        margin-bottom: 0.75rem;
        letter-spacing: 0.01em;
        width: fit-content;
      }

      .income-requirement-badge {
        display: inline-flex;
        align-items: center;
        align-self: flex-start;
        gap: 0.5rem;
        color: #198754;
        padding: 0;
        font-size: 0.88rem;
        font-weight: 600;
        margin-bottom: 0.85rem;
      }

      @keyframes pulse {
        0% {
          opacity: 0.4;
          transform: translateX(0);
        }
        50% {
          opacity: 1;
          transform: translateX(6px);
        }
        100% {
          opacity: 0.4;
          transform: translateX(0);
        }
      }
    </style>

    <div class="container hide-scrollbar" style="overflow-y:scroll; overflow-x:hidden;">
      <div class="row gy-4">
        <div id={sectionId} class="col-12 section-spacer">
          <h3 class="text-center">{title}</h3>
          <hr />
          <p class="text-end scroll-indicator">
            Scroll For More <i class="bi bi-chevron-double-right" style="font-size: 1rem;"></i>
          </p>
        </div>

        <div class="row flex-nowrap overflow-x-scroll hide-scrollbar pb-3">
          {recommendedCards.map((card) => (
            <div class="col-lg-4 col-md-6 flex-shrink-0">
              <article class="position-relative h-100">
                <div class="post-img position-relative overflow-hidden">
                  <img src={card.imageUrl} class="img-fluid" alt={card.name} style="border-radius: 12px;" />
                  <span class="post-date">
                    {new Date(card.lastUpdateDate).toLocaleDateString('en-GB', {
                      day: 'numeric',
                      month: 'short',
                      year: 'numeric'
                    })}
                  </span>
                </div>
                <div class="post-content d-flex flex-column">
                  <p class="recommendation-badge mb-2">
                    <i class="bi bi-hand-thumbs-up-fill" aria-hidden="true"></i>
                    Highly Recommended
                  </p>
                  <h3 class="post-title">
                    Unlock Amazing Benefits with the <span class="fw-bold text-success">{card.name}</span>!
                  </h3>
                  {card.highlights && card.highlights.length > 0 && (
                    <ul>
                      {card.highlights.map((item) => <li>{item}</li>)}
                    </ul>
                  )}
                  {card.annualIncomeRequirementRM && (
                    <p class="income-requirement-badge">
                      <i class="bi bi-coin" aria-hidden="true"></i>
                      {formatAnnualIncomeRequirement(card.annualIncomeRequirementRM)}
                    </p>
                  )}
                  <hr />
                  <a href={card.localPageUrl} class="readmore stretched-link">
                    <span>Learn More</span><i class="bi bi-arrow-right"></i>
                  </a>
                </div>
              </article>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>
)}
